// 修复记忆库
// AI 会在修复代码时参考这里的经验
window.MERMAID_FIX_MEMORY = [
    {
        summary: "子图（subgraph）的结束标记必须与开始标记匹配，且子图名称不能包含空格，应使用下划线或驼峰命名。"
    },
    {
        summary: "节点名称中包含空格（如 'Process Data'），而 Mermaid 不允许节点标识符中存在空格。修复方法是将空格替换为下划线（如 'Process_Data'），使节点名称合法。"
    },
    {
        summary: "子图 `subgraph Group1` 缺少对应的 `end` 语句，导致解析器在遇到后续节点连接 `A --> D` 时无法正确识别结构。修复方法是在 `Group2` 的 `end` 后补充 `Group1` 的 `end`，以正确闭合嵌套的子图。"
    },
    {
        summary: "节点标签中包含特殊字符（如 `/`、`(`、`)`、`χ²` 等）时未使用双引号包裹，导致 Mermaid 解析失败。修复方法是将所有节点标签用双引号 `\"` 明确包裹，确保特殊字符被正确识别为文本内容。"
    },
    {
        summary: "Mermaid 节点文本中包含未转义的特殊字符（如 `|` 和 `\n`）导致解析失败。修复方法是将所有节点标签用双引号包裹，以正确处理其中的特殊字符和换行符。"
    },
    {
        summary: "节点文本中包含特殊字符（如 `/`、`↑`、`↓`、`→` 等）且未用引号包裹，导致 Mermaid 解析器误判语法。修复方法是将所有节点标签内容用双引号 `\"` 显式包裹，确保特殊字符被正确识别为文本。"
    }
];
